package unsw.loopmania;

import java.util.ArrayList;

// simple interface for lambda expression to switch a menu... no args required...
public interface MenuSwitcher {
    public void switchMenu(ArrayList<String> mode);
}
